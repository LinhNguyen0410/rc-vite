import styled from 'styled-components';
import { Box, Grid } from '@mui/material';

export const ProductListStyled = styled(Grid)`
  display: flex;
  flex-wrap: wrap;
  justify-content: space-between;
  gap: 30px;
`;
